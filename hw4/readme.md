# Домашнее задание для лекции по теме "Системный вызов futex, устройство кэша, модель памяти C++"

В этой домашней работе два варианта:

* Вариант #1 -- реализовать аналог std::mutex с использованием системного вызова futex.

* Вариант #2 -- реализовать аналог std::condition_variable с использованием системного вызова futex.

Номер варианта вычисляется как остаток от деления на 2 вашего номера в списке группы.

## Сборка и запуск тестов

```bash
cmake -S . -B build
cmake --build build -j
ctest --test-dir build --output-on-failure
```
